language: python
python:
  - 3.7
install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
  - pip install codecov
script: pytest bioinformatics_stronghold/*.py
after_success:
  - codecov # submit coverage